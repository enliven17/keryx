# dependencies & build output
node_modules/
dist/
out/
cache/
broadcast/
.next/
*.vsix
*.tsbuildinfo

# Foundry dependencies are installed by contracts/script/install-deps.sh.
/contracts/lib/
/lib/

# secrets & deployment keys (regenerated by the deploy scripts)
.env
.env.local
.env.*.local
contracts/deployments/

# local runtime state (seeded demo context)
server/data/

references/

.DS_Store
